<!-- @user.my_articles permet de voir ce que l'on a en vente
@user.my_purchases permet de voir ce que l'on a déjà acheté -->
<div class="d-flex flex-column align-items-start gap-2">
    <h3 class="fw-bold"><%= @user.first_name %> <%= @user.last_name %></h3>
    <p class="text-muted"><%= @user.description %></p>
    <%= link_to "Editer", edit_user_path(@user), class:"btn btn-primary btn-lg" %>
</div>